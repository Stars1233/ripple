---
'@tsrx/ripple': patch
'ripple': patch
'@ripple-ts/vite-plugin': patch
---

Update `@tsrx/core` to ^0.2.3 and use its SVG and MathML tag-name predicates
for dynamic class handling, removing the compiler's duplicate tag-name list.
