{
  "name": "svelte-effectful-list-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "effectful-list benchmark fixture for Svelte 5.",
  "scripts": {
    "dev": "vite --host --port 5277 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5277 --strictPort"
  },
  "dependencies": {
    "svelte": "catalog:benchmarks"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "catalog:benchmarks",
    "vite": "catalog:benchmarks"
  }
}
