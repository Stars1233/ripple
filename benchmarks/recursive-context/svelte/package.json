{
  "name": "svelte-recursive-bench",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "recursive-context benchmark fixture for Svelte 5.",
  "scripts": {
    "dev": "vite --host --port 5275 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5275 --strictPort"
  },
  "dependencies": {
    "svelte": "catalog:benchmarks"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "catalog:benchmarks",
    "vite": "catalog:benchmarks"
  }
}
