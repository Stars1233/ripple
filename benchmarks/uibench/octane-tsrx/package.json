{
  "name": "octane-tsrx-uibench-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Octane TSRX implementation of the UIbench desktop operation matrix.",
  "scripts": {
    "dev": "vite --host --port 5315 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5315 --strictPort",
    "typecheck": "tsrx-tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "octane": "0.2.6"
  },
  "devDependencies": {
    "vite": "catalog:benchmarks"
  }
}
