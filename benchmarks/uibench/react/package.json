{
  "name": "react-uibench-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "React Compiler control for the UIbench desktop operation matrix.",
  "scripts": {
    "dev": "vite --host --port 5316 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5316 --strictPort"
  },
  "dependencies": {
    "react": "catalog:benchmarks",
    "react-dom": "catalog:benchmarks"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "catalog:benchmarks",
    "vite": "catalog:benchmarks"
  }
}
