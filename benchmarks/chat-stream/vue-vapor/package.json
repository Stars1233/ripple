{
  "name": "vue-vapor-chat-stream",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Streaming-chat fixture for Vue Vapor (3.6 vapor mode) - the Vue column for the deterministic token-stream ops.",
  "scripts": {
    "dev": "vite --host --port 5254 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5254 --strictPort"
  },
  "dependencies": {
    "vue": "catalog:benchmarks",
    "@vue/runtime-dom": "catalog:benchmarks",
    "@vue/runtime-vapor": "catalog:benchmarks"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "catalog:benchmarks",
    "vite": "catalog:benchmarks"
  }
}
