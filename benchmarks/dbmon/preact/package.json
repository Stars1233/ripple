{
  "name": "preact-dbmon-bench",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "dbmon update-stress benchmark fixture for Preact.",
  "scripts": {
    "dev": "vite --host --port 5263 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5263 --strictPort"
  },
  "dependencies": {
    "preact": "catalog:benchmarks"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:benchmarks",
    "vite": "catalog:benchmarks"
  }
}
