{
  "name": "ripple-dbmon-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "dbmon update-stress bench fixture for ripple - exposes window.__mount/__tick/__tickPartial/__remount/__sort/__unmount/__reset for the Playwright harness.",
  "scripts": {
    "dev": "vite --host --port 5199 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5199 --strictPort"
  },
  "dependencies": {
    "ripple": "workspace:*"
  },
  "devDependencies": {
    "@ripple-ts/vite-plugin": "workspace:*",
    "vite": "catalog:benchmarks"
  }
}
