{
  "name": "ripple-weather-app-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Native Ripple weather application benchmark fixture.",
  "scripts": {
    "dev": "vite --host --port 5298 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5298 --strictPort"
  },
  "dependencies": {
    "ripple": "workspace:*"
  },
  "devDependencies": {
    "vite": "catalog:benchmarks",
    "@ripple-ts/vite-plugin": "workspace:*"
  }
}
