{
  "name": "svelte-portal-swarm-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "portal-swarm benchmark fixture for Svelte 5.",
  "scripts": {
    "dev": "vite --host --port 5279 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5279 --strictPort"
  },
  "dependencies": {
    "svelte": "catalog:benchmarks"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "catalog:benchmarks",
    "vite": "catalog:benchmarks",
    "terser": "catalog:benchmarks"
  }
}
