{
  "name": "svelte-jsbench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "js-framework-benchmark fixture for Svelte 5 (keyed runes implementation).",
  "scripts": {
    "dev": "vite --host --port 5271 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5271 --strictPort"
  },
  "dependencies": {
    "svelte": "catalog:benchmarks"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "catalog:benchmarks",
    "vite": "catalog:benchmarks"
  }
}
