{
  "name": "inferno-svg-dashboard-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Native Inferno SVG dashboard fixture with memoized class boundaries and portal updates.",
  "scripts": {
    "dev": "vite --host --port 5324 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5324 --strictPort"
  },
  "dependencies": {
    "inferno": "catalog:benchmarks",
    "inferno-create-element": "catalog:benchmarks"
  },
  "devDependencies": {
    "esbuild": "catalog:benchmarks",
    "vite": "catalog:benchmarks"
  }
}
