{
  "name": "svelte-svg-dashboard-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "svg-dashboard SVG render/update stress bench fixture for Svelte 5 - exposes the shared window.__mount/__tick/... hook contract for the Playwright harness.",
  "scripts": {
    "dev": "vite --host --port 5305 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5305 --strictPort"
  },
  "dependencies": {
    "svelte": "catalog:benchmarks"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "catalog:benchmarks",
    "esbuild": "catalog:benchmarks",
    "vite": "catalog:benchmarks"
  }
}
